# Face-Control Input

A small Python script that uses MediaPipe Face Mesh + OpenCV to track nose movement and trigger keyboard inputs through pyautogui.

## Run

pip install -r requirements.txt  
python main.py

Press 'q' to exit.
